{
  "name": "service-ui",
  "version": "1.0.0",
  "scripts": {
    "build": "echo Building... && mkdir -p build && cp -r src/* build/"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}

